name: Test
on:
  push:
    branches: [ test ]
    paths:
      - 'src/**'
      - '.github/workflows/**'
  pull_request:
    branches: [ test ]
    paths:
      - 'src/**'
      - '.github/workflows/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Docker And Run Container With SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TEST_HOST }}
          username: ${{ secrets.TEST_USER }}
          key: ${{ secrets.TEST_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd ~/dingtalk-springboot/
            git pull origin master
            mvn package
            docker build -t dingtalk_springboot .
            docker container stop dingtalk_springboot
            docker container rm dingtalk_springboot
            docker run -d --net dingtalk-net -p 8080:8080 --name dingtalk_springboot -v /etc/timezone:/etc/timezone -v /etc/localtime:/etc/localtime -e PROFILES="test" dingtalk_springboot
            docker image prune -f

